<스택 클래스 설명>
스택클래스 안에서 쓰이는 배열과 위치를 알려주는 top이라는 변수를 생성해줍니다

Stack 함수는 생성자이며 size값을 받으면 배열의 사이즈를 size값으로 만든뒤 top의 값을 -1로 초기화 합니다

push 함수는 만약 스택이 가득차면 스택이 가득 찼다는 메세지를 입력하고 그렇지 않다면 현재 비어있는 공간에 전달받은 item 값을 넣습니다.

peek 함수는 현재 top이 가리키는 인덱스 위치의 값을 반환합니다.

pop 함수는 만약 스택이 비어있으면 top이 가리키는 위치의 인덱스 값을 제거하고 가리키는 위치를 top--한 위치로 옮깁니다.

isEmpty() 와 isFull() 함수들은 각각 배열이 비어있는지 꽉차있는지의 여부를 확인하는 함수입니다.

print() 함수는 현재 배열의 모든 값을 출력하는 함수이고 만약 비어있다면 "스택이 비어있다는 메세지를 출력합니다.

<메인 클래스 설명>
먼저 스택의 사이즈를 입력 받고 스택 클래스를 참조하여 스택 배열을 생성합니다.
그리고 메뉴를 입력받은뒤 스위치문을 이용하여 push pop print를 실행합니다 4를 누르면 종료이고 그외에는 다시 입력하라는 메세지를 출력합니다.
